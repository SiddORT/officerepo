Build a production-ready SaaS foundation named "Office Repo" using:

Frontend (Web):
React + TailwindCSS

Backend (API-first):
FastAPI (Python)

Mobile Support:
Backend must support both Web and Mobile clients using the same API layer.
Design authentication and routing mobile-compatible from day one.

Database:
PostgreSQL

Architecture Type:
Multi-tenant SaaS with database-per-tenant architecture and a central platform database.

Repository Structure:

office-repo/
frontend-web/
backend/
mobile-ready-api/
scripts/
docs/

Backend Architecture Requirements:

Create FastAPI backend with modular architecture:

backend/app/

modules/
auth/
employee/
hrms/
assets/
billing/

platform/
tenants/
subscriptions/
tenant_domains/
idp_config/
feature_flags/

core/
tenant_resolver.py
db_router.py
security.py
middleware.py

shared/
permissions/
notifications/
audit/
storage/
utils/

config/
database/

Create FastAPI entry file:

main.py

Platform Database Requirements:

Create models for:

tenants
tenant_domains
tenant_db_connections
subscriptions
plans
tenant_idp_configs
feature_flags
mobile_device_sessions
superadmins

Platform database stores tenant metadata only.

Tenant Resolver Middleware Requirements:

Create middleware that:

detects tenant from JWT token OR subdomain OR request header
loads tenant database connection dynamically
attaches tenant_db connection to request.state

Support both:

Web requests
Mobile API requests

Central Identity Service Requirements:

Create authentication module supporting:

login
logout
refresh token
JWT token generation
role-based access control
tenant_id inside JWT payload
device session tracking for mobile login

JWT payload example:

{
user_id: int
tenant_id: string
role: string
device_type: "web" | "mobile"
}

Mobile Authentication Requirements:

Support mobile-safe login architecture:

refresh tokens
access tokens
device session storage
logout from device
multiple device sessions per user

Create table:

mobile_device_sessions

Tenant Databases Requirements:

Tenant databases store business data such as:

employees
departments
attendance
assets
leave_requests
billing
roles
permissions

Do NOT store tenant metadata here.

Superadmin Panel APIs:

Create APIs for:

create tenant
list tenants
activate tenant
suspend tenant
assign subscription plan
configure tenant database connection
configure tenant SSO provider
enable or disable modules per tenant

Feature Flag System Requirements:

Create module enable/disable system per tenant:

Example:

Tenant A:
HRMS enabled
Assets enabled

Tenant B:
HRMS enabled
Billing enabled

Create table:

feature_flags

Mobile API Readiness Requirements:

Design backend APIs mobile-first compatible:

stateless authentication
JWT access tokens
refresh tokens
device tracking
pagination support
API versioning:

/api/v1/

React Frontend Requirements:

Create React frontend with Tailwind configured.

Create structure:

src/pages/

login
dashboard
superadmin

src/services/

apiClient.js

src/components/

Create Axios interceptor that automatically attaches JWT token.

Create login page UI.

Create placeholder dashboard page.

Create superadmin tenant list page UI.

Mobile App Preparation Requirements:

Backend must support future mobile apps for:

Superadmin mobile app
Tenant admin mobile app
Employee mobile app

Ensure APIs support:

device login tracking
refresh token workflow
session expiration handling
push notification readiness

Environment Config Requirements:

Add environment variable support:

DATABASE_URL
PLATFORM_DB_URL
JWT_SECRET
REFRESH_SECRET
TENANT_RESOLVER_STRATEGY

API Versioning Requirement:

Create versioned API routes:

/api/v1/auth/login
/api/v1/tenants
/api/v1/employees

Documentation Requirements:

Generate README explaining:

multi-tenant architecture
platform DB vs tenant DB
tenant resolver logic
mobile authentication design
module structure

Important Architecture Rules:

Single backend instance serves all tenants
Each tenant uses separate database
Platform database stores tenant metadata
Tenant detection happens via middleware
Authentication is centralized
System supports web and mobile clients equally
System must be ready for future SSO integration
System must support module enable/disable per tenant
